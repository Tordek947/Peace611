/**
 * Contains 'simple' implementations of services. It means, that there are no
 * real calls to server, instead this calls are mocked and all the data is
 * stored in application memory directly using lists, maps or just plain fields.
 */
package ua.com.kl.cmathtutor.itcapp.service.simple;